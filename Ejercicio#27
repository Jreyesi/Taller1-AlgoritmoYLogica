Algoritmo Mayor_pos_vec
	Definir i,pos1, pos2, veca, vecb, vecc,a,b Como Entero
	Dimension veca[15], vecb[15], vecc[15]
	pos1 = 1;
	Para x=1 Hasta 15 Con Paso 1 Hacer
		Escribir  "Ingrese el valor del vector A en la posición: ",  pos1;
		leer veca(x);
		Escribir  "Ingrese el valor del vector B en la posición: ", pos1;
		leer vecb(x);
		pos1=pos1+1
	Fin Para
	Para x=1 Hasta 15 Con Paso 1 Hacer
		vecc(x) = veca(x) + vecb(x)
		si vecc(x)>i Entonces
			i=vecc(x);
		FinSi
	Fin Para
	Para x=1 Hasta 15 Con Paso 1 Hacer
		pos2 = pos2 +1;
		Escribir  "La suma de sus vectores es: ", "Vector C [", pos2, "] = ", vecc(x);
	Fin Para
	Escribir "El numero mayor del vector C es: ",i;
FinAlgoritmo
